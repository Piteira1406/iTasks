rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Acesso total para todos autenticados (temporário para debug)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}