rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Esta regra diz: "Qualquer pessoa com Login feito pode ler e escrever tudo"
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
    
  }
}